Spanner uses TrueTime, a globally synchronized clock service, to assign timestamps to transactions. This ensures a consistent ordering of operations even across geographically dispersed nodes, aiding consistency during potential partitions. However, the article clarifies that TrueTime itself doesn't directly achieve CA.The article clarifies that TrueTime's primary function isn't to directly achieve CA. Instead, it offers several benefits: Efficient Timestamping: TrueTime facilitates assigning monotonically increasing timestamps, crucial for maintaining data order and enabling consistent reads across the system. Strong Reads: Timestamps enable strong reads, which guarantee a consistent view of data as of a specific point in time, even across geographically spread replicas. The enables versioning vectors that are useful in recovering partitions at a joint access point or merge.
